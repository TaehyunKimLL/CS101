---
marp: true
---

# 프로그래밍 입문 (Python)
## VS Code로 Python 개발 환경 구축 (Windows)

---

## 1. 오늘 목표

- Python 설치/확인
- VS Code에서 Python 개발 준비
- 프로젝트별 가상환경(venv) 구성
- 실행 / 디버깅(F5)
- 패키지 설치 & requirements.txt
- (선택) Jupyter Notebook

---

## 2. 왜 “개발 환경”이 중요한가?

- 코딩 실력의 발목을 잡는 1순위: **환경 문제**
- 같은 코드라도 환경이 다르면 결과가 달라질 수 있음
- 프로젝트별로 패키지/버전이 달라짐

👉 **표준 루틴(venv + requirements.txt)** 을 익히면 빨라진다

---

## 3. 구성 요소 한 장 요약

- Python (인터프리터)
- VS Code (에디터)
- Extensions (Python / Pylance / Jupyter)
- venv (프로젝트별 격리)
- pip (패키지 설치)
- Debugger (F5)

---

## 4. Python 설치 경로 2가지

### A) Microsoft Store
- 초심자에게 쉬움
- 자동 업데이트

### B) python.org
- 표준 설치
- 설치 옵션(예: PATH 추가) 주의

---

## 5. 설치 확인: 터미널 명령

```
python --version
python -m pip --version
```

환경에 따라:
```
py --version
```

---

## 6. VS Code 확장(Extensions)

필수:
- Python (Microsoft)
- Pylance (Microsoft)

선택:
- Jupyter (노트북)

---

## 7. 프로젝트 시작 루틴

1) 폴더 만들기
2) VS Code로 열기: `code .`
3) venv 만들기: `python -m venv .venv`
4) 활성화: `Activate.ps1` / `activate.bat`
5) 인터프리터 선택: `.venv`

---

## 8. venv는 왜 쓰나?

- 프로젝트마다 필요한 패키지/버전이 다름
- 시스템 Python이 오염되는 것을 방지
- “내 컴퓨터에선 되는데?” 문제 감소

👉 venv = **프로젝트 전용 실험실**

---

## 9. 인터프리터 선택이 중요한 이유

VS Code는 “어느 Python”으로 실행할지 알아야 함

- 시스템 Python
- Store Python
- 프로젝트 .venv Python

👉 `.venv` 선택 = 실행/디버깅/노트북이 같은 환경을 사용

---

## 10. 첫 실행 & 디버깅

실행:
```
python hello.py
```

디버깅:
- 중단점(Breakpoint)
- F5
- Variables / Call Stack 확인

👉 “어디서 값이 이상해졌는지” 찾는 도구

---

## 11. pip & 패키지 설치

```
python -m pip install requests
python -m pip show requests
```

왜 `python -m pip`를 쓰나?
- “현재 선택된 Python/venv의 pip”를 확실히 사용하기 위해

---

## 12. requirements.txt

생성:
```
python -m pip freeze > requirements.txt
```

다른 PC에서 재현:
```
python -m pip install -r requirements.txt
```

👉 “이 프로젝트는 이런 패키지가 필요해요” 목록

---

## 12-추가. pip 대신 **uv** 워크플로우 (빠르고 재현성 좋음)

설치(예: Scoop):
```powershell
scoop install main/uv
```

프로젝트 루틴:
```powershell
uv init
uv add requests
uv run python hello.py
uv sync
```

- `uv add`: 의존성 선언 + lock + 설치까지 포함 
- `uv sync`: lock 기준으로 환경을 정확히 동기화 

---


## 13. 코드 품질: 포매터/린터

- Formatter: 자동 정렬(예: Black)
- Linter: 문제/규칙 검사(예: Ruff)

목표:
- “돌아가기만 하는 코드”에서
- “읽기 쉬운 코드”로

---

## 14. (선택) Jupyter Notebook

- `.ipynb` 파일
- 셀 단위 실행
- 데이터/실험에 강함

중요: Kernel을 `.venv`로 선택하기

---

## 15. 흔한 문제와 해결

- `python`이 안 잡힘 → 설치/ PATH / `py` 사용
- venv 활성화가 안 됨 → 터미널 종류 변경(CMD) or 정책 설정
- VS Code가 다른 Python을 씀 → 인터프리터 다시 선택
- pip가 이상함 → `python -m pip`로 통일

---

## 16. 마무리

오늘의 핵심 3가지
1) 프로젝트마다 `.venv`
2) VS Code 인터프리터는 `.venv`
3) 패키지는 `requirements.txt`로 관리

다음: 문법/자료형/조건문부터 본격 코딩!
